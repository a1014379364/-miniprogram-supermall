<!--pages/cart/cart.wxml-->
<view class="cart">
  <!--购物车列表-->
  <cart-list cartList="{{cartList}}" bind:isAllClick="listClick" selected="{{selected}}"/>

  <!--底部导航-->
  <bottom-control isAll="{{isAll}}" bind:selectClick="selectClick"/>
</view>
